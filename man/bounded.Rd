% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait-core.R
\name{bounded}
\alias{bounded}
\title{Constrain the upper and lower bounds of a type}
\usage{
bounded(type, left = NULL, right = NULL, bounds = "[]")
}
\arguments{
\item{type}{A type.}

\item{left, right}{Scalar values giving the left and right bounds. Either may be \code{NULL} (the default)
to leave that side unbounded. At least one of \code{left}, \code{right} must be supplied.}

\item{bounds}{A string denoting the boundary type: \code{"[]"} (closed, the default), \code{"[)"}, \code{"(]"}, or \code{"()"} (open).

[)"\verb{, }"(]: R:)\%22\%60,\%20\%60\%22(}
}
\value{
A copy of \code{type} with an additional bound constraint.
}
\description{
\code{bounded()} returns a copy of \code{type} that requires objects to fall within a
range. Bounds are specified by \code{left} and \code{right}, and the interval type is
controlled by \code{bounds}:\tabular{lll}{
   Bounds \tab Interval \tab Condition \cr
   "[]" \tab Closed \tab left <= x & x <= right \cr
   "[)" \tab Right-open \tab left <= x & x < right \cr
   "(]" \tab Left-open \tab left < x & x <= right \cr
   "()" \tab Open \tab left < x & x < right` \cr
}


Either \code{left} or \code{right} may be omitted to leave that side unbounded:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{t_dbl |> bounded(0, 1)             # probabilities: [0, 1]
t_dbl |> bounded(0, bounds = "()") # strictly positive
t_chr |> bounded("m", "p")         # starts with "m", "n", "o", or "p"
}\if{html}{\out{</div>}}

\code{bounded()} ignores \code{NA} values when checking bounds. To require
non-missingness, combine with the \code{complete()} trait.

[)"   | Right-open   | left <= x & x < right  |
| "(]: R:)\%22\%20\%20\%20\%7C\%20Right-open\%20\%20\%20\%7C\%20left\%20\%3C=\%20x\%20&\%20x\%20\%3C\%20right\%20\%20\%7C\%0A\%7C\%20\%22(
[0, 1]: R:0,\%201
}
\examples{
t_prob <- t_dbl |> bounded(0, 1)
t_positive <- t_dbl |> bounded(0, bounds = "()")

obj_inspect_type(0.5, t_prob)
obj_inspect_type(0, t_positive)

# `NA` values are considered within bounds
obj_is_type(c(0, NA, 0.75), t_prob)

}
\seealso{
\code{\link[=sized]{sized()}} for size constraints, \code{\link[=complete]{complete()}} for non-missingness.
}
