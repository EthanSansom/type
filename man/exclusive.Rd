% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait-relations.R
\name{exclusive}
\alias{exclusive}
\title{Require exactly one element or argument to be supplied}
\usage{
exclusive(...)
}
\arguments{
\item{...}{Inside \code{\link[=has_relation]{has_relation()}}, one or more selectors (e.g. \code{\link[=on_elm]{on_elm()}},
\code{\link[=on_elms]{on_elms()}}, \code{\link[=on_attr]{on_attr()}}, \code{\link[=on]{on()}}).

Inside \code{\link[=typed]{typed()}}, one or more argument names.}
}
\description{
\code{exclusive()} constrains a set of values so that exactly one is non-\code{NULL}
or non-missing. Its behaviour depends on context:
\itemize{
\item Inside \code{\link[=has_relation]{has_relation()}}: requires that exactly one of the selected elements or attributes of an object is non-\code{NULL}.
\item Inside \code{\link[=typed]{typed()}}: requires that exactly one of the function arguments is supplied.
}
}
\examples{
# Require that exactly one of elements `[["x"]]` and `[["y"]]` is non-NULL
t <- t_any |> has_relation(exclusive(on_elm("x"), on_elm("y")))
obj_inspect_type(list(x = 1L, y = NULL), t)
obj_inspect_type(list(x = NULL, y = NULL), t)
obj_inspect_type(list(x = 1L, y = 1L), t)

# Require that either `x` or `y` is supplied
f <- typed(
  exclusive(x, y),
  function(x = optional(t_any), y = optional(t_any)) { NULL }
)
f(x = 1L)
try(f())
try(f(x = 1L, y = "a"))

}
\seealso{
\code{\link[=typed]{typed()}} for typed function construction, \code{\link[=has_relation]{has_relation()}} for adding between-element type constraints.
}
