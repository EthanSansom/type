% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait-relations.R
\name{same_sized}
\alias{same_sized}
\title{Require elements to be the same size}
\usage{
same_sized(...)
}
\arguments{
\item{...}{Inside \code{\link[=has_relation]{has_relation()}}, one or more selectors (e.g. \code{\link[=on_elm]{on_elm()}},
\code{\link[=on_elms]{on_elms()}}, \code{\link[=on_attr]{on_attr()}}, \code{\link[=on]{on()}}).

Inside \code{\link[=typed]{typed()}}, one or more argument names.}
}
\description{
\code{same_sized()} constrains a set of values to share the same size, checked
via \code{\link[vctrs:vec_size]{vctrs::vec_size()}}. Its behaviour depends on context:
\itemize{
\item Inside \code{\link[=has_relation]{has_relation()}}: requires that the selected elements or attributes of an object all have the same size.
\item Inside \code{\link[=typed]{typed()}}: requires that the specified function arguments all have the same size at call time.
}
}
\examples{
# Require that elements `[["a"]]` and `[["b"]]` are the same size
t <- t_any |> has_relation(same_sized(on_elm("a"), on_elm("b")))
obj_inspect_type(list(a = 1:3, b = 1:3), t, obj_name = "obj")
obj_inspect_type(list(a = 1:3, b = 1:2), t, obj_name = "obj")
obj_inspect_type(list(1:2, b = 1:2), t, obj_name = "obj")

# Require that arguments `x` and `y` are the same size
f <- typed(same_sized(x, y), function(x = t_int, y = t_int) { NULL })
f(1:3, 1:3)
try(f(1:3, 1:2))

}
\seealso{
\code{\link[=typed]{typed()}} for typed function construction, \code{\link[=has_relation]{has_relation()}} for
adding between-element type constraints.
}
