% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait-relations.R
\name{relations}
\alias{relations}
\alias{same_classed}
\alias{same_sized}
\alias{recyclable}
\alias{exclusive}
\title{Set between-element type constraints}
\usage{
same_classed(...)

same_sized(...)

recyclable(...)

exclusive(...)
}
\arguments{
\item{...}{Inside \code{\link[=has_relation]{has_relation()}}, one or more selectors (e.g. \code{\link[=on_elm]{on_elm()}},
\code{\link[=on_elms]{on_elms()}}, \code{\link[=on_attr]{on_attr()}}, \code{\link[=on]{on()}}).

Inside \code{\link[=typed]{typed()}}, one or more arguments.}
}
\description{
Relations constrain how a set of values must relate to one another. Each
relation function's behaviour depends on context:
\itemize{
\item Inside \code{\link[=has_relation]{has_relation()}}: constrains selected elements or attributes of an object.
\item Inside \code{\link[=typed]{typed()}}: constrains the specified function arguments at call time.
}

The following relations are provided:
\itemize{
\item \code{same_sized()}: all values must share the same size, checked via \code{\link[vctrs:vec_size]{vctrs::vec_size()}}.
\item \code{same_classed()}: all values must share the same class.
\item \code{recyclable()}: all values must be size 1 or share the same size.
\item \code{exclusive()}: exactly one value must be non-\code{NULL} (in \code{\link[=has_relation]{has_relation()}}) or supplied (in \code{\link[=typed]{typed()}}).
}
}
\examples{
# Require elements be the same size
t <- t_any |> has_relation(same_sized(on_elm("a"), on_elm("b")))
obj_inspect_type(list(a = 1:3, b = 1:3), t, obj_name = "obj")
obj_inspect_type(list(a = 1:3, b = 1:2), t, obj_name = "obj")

# Require elements to share a class
t <- t_any |> has_relation(same_classed(on_elm(1L), on_elm(2L)))
obj_inspect_type(list(1L, 2L), t)
obj_inspect_type(list(1L, "a"), t)

# Require elements be size 1 or the same size
t <- t_any |> has_relation(recyclable(on_elm("x"), on_elm("y")))
obj_inspect_type(list(x = 1L, y = 1:3), t)
obj_inspect_type(list(x = 1:2, y = 1:3), t)

# Require that exactly one element must be non-NULL or supplied
t <- t_any |> has_relation(exclusive(on_elm("x"), on_elm("y")))
obj_inspect_type(list(x = 1L, y = NULL), t)
obj_inspect_type(list(x = 1L, y = 1L), t)

f <- typed(
  exclusive(x, y),
  function(x = optional(t_any), y = optional(t_any)) NULL
)
f(x = 1L)
try(f(x = 1L, y = "a"))

}
\seealso{
\code{\link[=has_relation]{has_relation()}} to attach a relation to a type, \code{\link[=typed]{typed()}} for typed function construction.
}
